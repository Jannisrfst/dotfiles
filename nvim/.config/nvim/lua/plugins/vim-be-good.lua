-- vim-be-good plugin for practicing vim movements
return {
  {
    "ThePrimeagen/vim-be-good",
    cmd = "VimBeGood", -- Lazy load on command
    config = function()
      -- Optional: Add any custom configuration here
      -- The plugin works out of the box without additional setup
    end,
  },
}
