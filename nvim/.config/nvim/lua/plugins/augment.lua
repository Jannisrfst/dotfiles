-- Augment Code plugin for AI-powered coding assistance
return {
  {
    "augmentcode/augment.vim",
    config = function()
      -- Optional: Add any custom configuration here
      -- The plugin works out of the box without additional setup
    end,
  },
}
